#!/bin/sh

SRCPATCH=/tmp/src/patch
SRCBIN=/tmp/src/bin


if [ -d ${SRCBIN} ]; then
  echo "############ In Bin Copeying : ) ############"

chmod +x ${SRCBIN}/*

cp -v ${SRCBIN}/* $HOME/broker/etc/

fi

exit 0
