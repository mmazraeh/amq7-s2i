#!/bin/sh

SRCPATCH=/tmp/src/patch
SRCBIN=/tmp/src/bin
ETCHOME=/home/jboss/broker/etc

if [ -d ${SRCBIN} ]; then
  echo "############ In Bin Copeying : ) ############"

chmod +x ${SRCBIN}/*

cp -v ${SRCBIN}/* $HOME

fi

exit 0
