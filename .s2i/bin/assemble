#!/bin/sh

SRCPATCH=/tmp/src/patch
SRCBIN=/tmp/src/bin
ETCHOME=/home/jboss/broker/etc

if [ -d ${SRCBIN} ]; then
  echo "############ In Bin Copeying : ) ############"

chmod +x ${SRCBIN}/*
chmod +x $ETCHOME/*

cp -v ${SRCBIN}/* $ETCHOME/

fi

exit 0
