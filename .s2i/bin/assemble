#!/bin/sh

SRCPATCH=/tmp/src/patch
SRCBIN=/tmp/src/bin
ETCHOME=$HOME/broker/etc/

if [ -d ${SRCBIN} ]; then
  echo "############ In Bin Copeying : ) ############"

chmod +x ${SRCBIN}/*

cp -v ${SRCBIN}/* $ETCHOME

fi

exit 0
